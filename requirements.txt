# chipocr 项目依赖
# 验证环境：conda ocr (Python 3.12.13, Windows 11)
# 安装命令：pip install -r requirements.txt

# Qt GUI 框架
PySide6==6.10.3

# 摄像头采集与图像处理
opencv-python==4.11.0.86

# 文本检测 DB 后处理多边形外扩
pyclipper==1.3.0.post6

# 数值计算（onnxruntime-gpu 1.20.1 要求 numpy < 2）
numpy==1.26.4

# ONNX 推理引擎（GPU 版，依赖 CUDA 12.x + cuDNN 9.x）
# 若无 GPU 可替换为：onnxruntime==1.20.1
onnxruntime-gpu==1.20.1

# Windows GPU 推理所需 CUDA/cuDNN DLL（session_utils.py 会注册 nvidia/*/bin）
nvidia-cuda-runtime-cu12==12.9.79
nvidia-cublas-cu12==12.9.2.10
nvidia-cudnn-cu12==9.21.1.3
nvidia-cufft-cu12==11.4.1.4
nvidia-curand-cu12==10.3.10.19
nvidia-cusolver-cu12==11.7.5.82
nvidia-cusparse-cu12==12.5.10.65
nvidia-cuda-nvrtc-cu12==12.9.86
nvidia-nvjitlink-cu12==12.9.86
